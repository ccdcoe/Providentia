- if policy(Network.new(exercise: @exercise)).create?
  - provide :new_url do
    = url_for [:new, @exercise, :network]

= render 'form'
- if @network.persisted?
  = render 'addressing'
  = render 'config_map'